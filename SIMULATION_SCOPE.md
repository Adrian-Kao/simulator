# 信義商圈交通政策模擬：固定範圍與資料規格

## 1. 模擬目標

評估交通政策對信義商圈核心區及周邊影響區的交通流、旅行時間、速度與壅塞程度之影響；政策不能只改善核心區而將壅塞轉移至周邊道路。

## 2. 空間範圍

### 核心政策區

以**市府路、松高路、松仁路、信義路五段**圍成的區域為主要政策實施範圍。

### 政策影響區

以**忠孝東路五段、基隆路一段、松德路、信義路五段**圍成（含邊界道路）的區域為政策影響觀測範圍。

資料下載可使用足以覆蓋影響區的 bounding box，但模擬、統計與政策套用必須依上述道路構成的 polygon 與道路邊界判定；不得將下載 bbox 直接視為政策區。

## 3. 可操作的政策範圍

### 道路與號誌

- 調整路口號誌相位的綠燈秒數、週期長度與時段計畫。
- 新增、刪除或調整道路管制，例如特定時段禁止右轉、禁止左轉或禁止通行。

### YouBike

- 新增或移除站點。
- 調整站點車柱容量、初始自行車數量與調度規則。
- 觀察站點周轉率：指定時段的借車與還車總次數，除以可用車柱容量。

### 個人運具與停車

- 新增、移除或遷移停車場；調整位置、容量與費率。
- 調整紅線與臨停空間的位置、長度、允許時段與容量。

所有政策都必須以 machine-readable JSON 記錄，包含空間對象、有效時段、參數值、限制條件與版本。

## 4. 核心 KPI

每個 5 分鐘 tick，分別輸出核心政策區與政策影響區的下列數據：

| KPI | 定義 |
| --- | --- |
| Travel Time | 路段或 OD 路徑的平均旅行時間。 |
| Travel Speed | 路段平均速度；同時保存自由流速度比。 |
| 壅塞程度 | 優先使用 V/C（流量／容量）與 Travel Time Index，並分成暢通、輕度、中度與嚴重。 |

建議輔助 KPI：路口延滯、排隊長度、停車搜尋時間、YouBike 借還成功率、站點周轉率及運具轉移比例。

## 5. 模型邊界

- Simulation agents 以規則、效用函數與機率模型決策；不得讓每位 agent 呼叫 LLM。
- LLM 僅可用於政策文字結構化、政策結果診斷與產生受限的候選方案。
- 同一資料版本、政策版本、agent population 版本與 random seed 必須能重跑得到相同結果。
- 模型校準與驗證以真實資料及統計指標完成，不得以 LLM 判定準確性。

## 6. 歷史典型日與政策比較

第一版採用歷史資料而非即時決策。依 `weekday`、`weekend`、`event` 分別建立每個 5 分鐘 time slot、路段與方向的典型日 baseline；政策結果必須與同一典型日對照，不得直接拿任兩個日期比較。

模型校準與驗證資料需區分：

- 校準集：用於估計容量、速度、轉向比例與行為參數。
- 驗證集：保留未參與校準的日期，僅用於檢驗誤差。
- 活動日：獨立標示與建模，不混入一般平日／假日平均。

## 7. 最低資料需求

1. 道路與路口：幾何、方向、車道、速限、轉向限制、容量及號誌相位／秒數。
2. 分時交通：路段流量、旅行時間或速度，並標示平假日、天氣與活動。
3. 停車與臨停：位置、容量、費率、規則與分時使用率。
4. YouBike：站點、車柱容量、可借／可還數、借還紀錄及調度資料。
5. 大眾運輸：捷運與公車站點、路線／班距及分時運量。

現階段下載的開放資料僅作為靜態地理與即時快照的基礎；未具備歷史分時資料前，不得宣稱模型已完成校準或驗證。

不依賴ai 的 agent 測試：
```
python3 run_agent.py
```

simulation 每個 .py 獨立測試：
```
python3 -m unittest discover -s tests -v
```
